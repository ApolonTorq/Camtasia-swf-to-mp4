# Install optional dependencies for all platforms
optional=true

# Force installation of optional dependencies even if they fail
optional-dependencies=true

# Allow installation of packages for different platforms
engine-strict=false

# Ignore platform and architecture restrictions
target_platform=*
target_arch=*

# Force installation of all packages regardless of platform
force=true

# Suppress npm warnings
loglevel=error
